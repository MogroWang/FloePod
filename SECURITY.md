# 安全说明

请通过 GitHub Security Advisory 私下报告可能导致文件丢失、任意路径访问、敏感数据泄露或加密绕过的问题，不要在公开 Issue 中附带真实敏感文件、路径、日志或凭据。

## 安全边界

- FloePod 是本地文件工具，不是合规归档系统或零知识保险库；
- 敏感匣依赖 Windows EFS、Windows Hello/PIN 和操作系统账户安全；
- 可信交接包的 SHA-256 用于内容一致性验证，不证明发送者身份或法律效力；
- 隐私扫描属于提示工具，不能保证发现所有个人信息；
- 默认不上传遥测或文件数据。

## 发布要求

正式 Release 应通过锁定依赖的测试、Windows 构建、最终资产 SHA-256、GitHub 构建来源证明和 Authenticode 验证。没有配置真实证书的构建不得标记为“已签名”。
