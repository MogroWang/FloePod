# 更新日志

记录每个版本的显著变更，最新版本在最上面。

## 1.3.0（当前）

### 新增

- **自动屏蔽**：新增设置栏目。把游戏或全屏应用加入屏蔽列表后，这些应用位于前台时自动隐藏全部浮匣，切回其他应用后自动恢复；支持从文件选择器挑选程序或手动输入进程名，匹配不区分大小写
- **匣自动收起设置**：每个匣可单独开关「自动收起」并调节「收起延迟」；关闭后面板等价于固定，鼠标移开不再自动收回，只有手动收起才会关闭
- **面板独立外观**：面板新增独立的「面板材质」「面板填充色」与「面板填充色不透明度」，与胶囊条的外观设置彻底分开；旧配置升级时自动沿用原材质与不透明度，不会丢失设置
- **材质调整**：材质为亚克力、云母（Windows 11）、普通（原「纯半透明」更名）三种；原「模糊」与亚克力观感一致已合并，存量配置自动迁移为亚克力；材质现在同时作用于胶囊条与面板，选普通即为无系统材质的纯半透明
- **位置示意图自由拖动**：匣设置里的屏幕示意图放大，小蓝点可以直接拖到任意屏幕边缘定位（自动切换上 / 下 / 左 / 右），也支持方向键微调，与「沿边缘位置」滑杆实时联动；「屏幕边缘」选项顺序调整为上 / 下 / 左 / 右
- **删除匣可选方式**：删除匣时先选择「仅移除匣（保留文件夹和文件）」或「删除匣并把暂存文件夹与文件移入回收站」；选择后者会再次弹出原生确认，防止误删
- **颜色选取器确认按钮**：边框颜色与面板填充色改为先预览、点「确定」才生效；重置按钮同时清除未确认的预览
- **文字暂存改进**：面板尺寸在编辑文字时保持不变；新增「获取剪贴板」一键填入正文；标题留空时自动取正文第一行前 10 个字
- **文字暂存转场动画**：面板在列表与文字暂存之间切换时新增流畅的非线性转场（旧视图快速收起、新视图缓出上浮），动效偏好降级时自动简化

### 修复

- **胶囊条材质与圆角贴合（再次修复）**：胶囊条材质改走与窗口区域裁剪协同的合成路径（SWCA 亚克力）。此前 Windows 11 22H2+ 的系统背景材质铺满整个窗口矩形、不遵循裁剪出的胶囊区域，材质总在 CSS 圆角外露出直角；同时区域圆角改用目标显示器的缩放率计算，多显示器不同缩放时圆角不再错位
- **胶囊条标题栏伪影（再次修复）**：每次应用窗口区域前都幂等清理残留的标题栏样式位并关闭 DWM 非客户区渲染，焦点变化时也顺带清理，任何来源恢复的样式位都会被立即压掉；胶囊条窗口不再写入标题文字，即使短暂恢复非客户区渲染也不会显示出「匣名称」
- **失焦后材质消失**：Windows 11 的亚克力在窗口失焦时会被系统移除，现在聚焦状态变化时自动重放材质，无论聚焦与否始终显示底层材质
- **面板材质切换无效**：修复面板未固定（或隐藏）时切换材质不生效的问题，现在无论面板是否显示都会立即落地
- **右键菜单点击外部不关闭**：菜单窗口此前四周留有 12px 透明余量承载阴影，落在余量上的点击既不会让菜单失焦也不会作用于下层窗口，表现为「点左键菜单不消失」；现在窗口与菜单卡片完全同形并按卡片圆角裁剪，点击卡片外任意位置菜单都会关闭
- **右键菜单阴影显示错误**：彻底移除菜单卡片阴影（连同四周余量），不再出现阴影被窗口边缘裁切发灰的问题
- **右键菜单无法再次打开**：菜单窗口的显示改用与隐藏对称的原生 ShowWindow 路径。此前显示走 Tauri show()、隐藏走原生 SW_HIDE，WebView2 透明窗口的可见性状态错位，菜单首次关闭后就再也显示不出来

### 变更

- 匣的「边框颜色」在确认选色后才生效，选色过程中不再实时改变胶囊条外观
- 面板底部的「拖出时：复制 / 剪切」切换在匣为空时也会显示，不再依赖列表非空
- 设置侧栏新增「自动屏蔽」栏目，位于「常规」之后；各页描述文字精简，标题旁按钮不再被挤扁

## 1.2.0

### 新增

- **匣外观自定义**：每个匣新增「匣宽度」「圆角」「边框颜色」「边框不透明度」设置，胶囊条形状与边框随个人喜好调整；边框颜色默认跟随主题深浅色
- **新建匣弹窗**：点击「+ 新建匣」先在窗口内询问匣名称与文件夹位置，确认后才创建
- **匣名称编辑图标**：匣名称默认只读展示，点击旁边的编辑小图标才会进入重命名输入框（Enter 保存、Esc 取消）

### 变更

- **点击匣的逻辑重构**：面板显示中点击匣直接收起；面板未显示时点击匣以固定方式弹出，保持到再次点击匣或在面板上取消固定
- **匣管理按匣编辑**：存在多个匣时，设置页改为列表选择要调整的匣，只展示所选匣的设置，不再把所有匣铺开
- **匣面板背景一体化**：移除面板标题栏的独立底色，整块面板共用同一层可控透明度的背景
- **显示器标签编号化**：显示器下拉与示意图标签改为「显示器 N」，主显示器标记为「显示器 1（主）」，修复下拉里出现两个「主显示器」的问题
- 多选分段按钮（复制/剪切、落地动作等）的灰色轨道背景改用透明度调节，观感更浅更透气
- 「退出浮匣」按钮改为红色描边、悬停红色填充，更醒目
- 左上角 Logo 增加悬停放大提亮与按下回缩反馈
- 设置窗口标题栏文案改为「浮匣 FloePod 设置界面」
- 关于页描述改为按句号分行展示

## 1.1.0

### 新增

- **暂存条目右键菜单**：专属设计的置顶无边框菜单窗口，风格与面板一致
  - 单个条目：打开、打开所在位置、复制、复制文件路径、移出暂存
  - 多选条目：复制 N 项、复制 N 条路径、移出 N 项，批量操作一步到位
  - 文字条目显示「复制文字」，复制文本内容而非文件；全选文字时为「复制 N 段文字」
  - 复制路径为多行文本，可整批粘贴
- 右键选择行为与资源管理器一致：右键未选中的条目会先把选择收敛为该条目；右键已选中的条目则保留整批多选

### 变更

- 全局禁用 WebView 默认的右键菜单（重新加载 / 检查等英文菜单）
- 菜单打开期间面板不会被自动收起；同一时刻全局只保留一个菜单窗口，跨匣打开会先收起旧菜单

## 1.0.0

- 更新 FloePod 品牌图形，统一应用、安装包和便携包的版本信息

## 0.6.0

- 强化文件复制 / 移动 / 导出回滚与剪切拖出身份检查；修复面板状态竞态、首启幂等、多显示器缩放和安装版 / 便携版路径隔离；增加自动审计、Windows 构建与 Release 流水线

## 0.5.1

- 多匣事件定向化消除串扰；文字暂存支持文件标题；文件名截断放宽至 48 字符；启动预置主题防白闪 + 原生主题同步；面板 Windows 11 系统圆角

## 0.5.0

- 单一活动面板，看门狗改为直接隐藏避免重叠闪烁；暂存文件夹启动/变更对账；面板尺寸防抖；材质只应用一次减少重绘闪烁

## 0.4.0

- 引入多匣体系取代「场景」，每个匣独立配置位置、显示器与暂存文件夹
