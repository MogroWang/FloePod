# 更新日志

记录每个版本的显著变更，最新版本在最上面。

## 1.2.0（当前）

### 新增

- **匣外观自定义**：每个匣新增「匣宽度」「圆角」「边框颜色」「边框不透明度」设置，胶囊条形状与边框随个人喜好调整；边框颜色默认跟随主题深浅色
- **新建匣弹窗**：点击「+ 新建匣」先在窗口内询问匣名称与文件夹位置，确认后才创建
- **匣名称编辑图标**：匣名称默认只读展示，点击旁边的编辑小图标才会进入重命名输入框（Enter 保存、Esc 取消）

### 变更

- **点击匣的逻辑重构**：面板显示中点击匣直接收起；面板未显示时点击匣以固定方式弹出，保持到再次点击匣或在面板上取消固定
- **匣管理按匣编辑**：存在多个匣时，设置页改为列表选择要调整的匣，只展示所选匣的设置，不再把所有匣铺开
- **匣面板背景一体化**：移除面板标题栏的独立底色，整块面板共用同一层可控透明度的背景
- **显示器标签编号化**：显示器下拉与示意图标签改为「显示器 N」，主显示器标记为「显示器 1（主）」，修复下拉里出现两个「主显示器」的问题
- 多选分段按钮（复制/剪切、落地动作等）的灰色轨道背景改用透明度调节，观感更浅更透气
- 「退出浮匣」按钮改为红色描边、悬停红色填充，更醒目
- 左上角 Logo 增加悬停放大提亮与按下回缩反馈
- 设置窗口标题栏文案改为「浮匣 FloePod 设置界面」
- 关于页描述改为按句号分行展示

## 1.1.0

### 新增

- **暂存条目右键菜单**：专属设计的置顶无边框菜单窗口，风格与面板一致
  - 单个条目：打开、打开所在位置、复制、复制文件路径、移出暂存
  - 多选条目：复制 N 项、复制 N 条路径、移出 N 项，批量操作一步到位
  - 文字条目显示「复制文字」，复制文本内容而非文件；全选文字时为「复制 N 段文字」
  - 复制路径为多行文本，可整批粘贴
- 右键选择行为与资源管理器一致：右键未选中的条目会先把选择收敛为该条目；右键已选中的条目则保留整批多选

### 变更

- 全局禁用 WebView 默认的右键菜单（重新加载 / 检查等英文菜单）
- 菜单打开期间面板不会被自动收起；同一时刻全局只保留一个菜单窗口，跨匣打开会先收起旧菜单

## 1.0.0

- 更新 FloePod 品牌图形，统一应用、安装包和便携包的版本信息

## 0.6.0

- 强化文件复制 / 移动 / 导出回滚与剪切拖出身份检查；修复面板状态竞态、首启幂等、多显示器缩放和安装版 / 便携版路径隔离；增加自动审计、Windows 构建与 Release 流水线

## 0.5.1

- 多匣事件定向化消除串扰；文字暂存支持文件标题；文件名截断放宽至 48 字符；启动预置主题防白闪 + 原生主题同步；面板 Windows 11 系统圆角

## 0.5.0

- 单一活动面板，看门狗改为直接隐藏避免重叠闪烁；暂存文件夹启动/变更对账；面板尺寸防抖；材质只应用一次减少重绘闪烁

## 0.4.0

- 引入多匣体系取代「场景」，每个匣独立配置位置、显示器与暂存文件夹
